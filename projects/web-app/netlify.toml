[build]
  command = "npm run build-netlify"
  functions = "functions"
  ignore = "/bin/false"
  publish = "dist"

[dev]
  command = "npm run serve"
  publish = "src"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

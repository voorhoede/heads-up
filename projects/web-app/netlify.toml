[build]
  command = "npm run postinstall && npm run build"
  functions = "functions"
  publish = "dist"
  ignore = "git diff --quiet HEAD^ HEAD -- projects/web-app"

[dev]
  command = "npm run serve"
  publish = "src"
